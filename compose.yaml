services:
  subscription-dock:
    build: .
    container_name: subscription-dock
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "127.0.0.1:3537:3537"
    volumes:
      - ./data:/app/data
